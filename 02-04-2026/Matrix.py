# [[10,35,11],
# [47,17,31]]


def is_Matrix(arr: list[int]):
    if len(arr) == 0:
        return "Invalid Input"
    else:
        result = []
        for i in range(0, len(arr[0]),+1):
            print(arr[i])
            # for j in range(0, len(arr[i]), +1):
                # print(arr[i])
                


is_Matrix([[10, 35, 11], [47, 17, 31]])
